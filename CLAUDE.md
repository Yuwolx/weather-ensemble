# CLAUDE.md — weather-ensemble 이어작업 지침

주력 기기는 맥북프로 M5. 이 레포를 clone하면 이 파일로 이어서 파악한다.

## 이 앱이 존재하는 이유 (설계 의도)

- 기상청 단일 예보 불신 → **여러 독립 모델의 앙상블 멤버를 그대로 세어 확률 분포로** 보여준다.
- 하나의 숫자로 뭉개지 않는다. "40% 안 옴 / 35% 약한 비 / 25% 강한 비"처럼 시나리오를 전부 편다.
- **모델 불일치 = 핵심 정보.** 막대가 여러 색으로 쪼개질수록 예보가 불확실하다는 뜻.
- 디자인 언어: Linear DESIGN.md 기반(근-검정 캔버스, 서피스 사다리, 희소한 라벤더 액센트).
  숫자는 전부 모노스페이스(계기판 느낌). 참고: `Desktop/권혁준/awesome-design-md`.

## 아키텍처 원칙 (지킬 것)

- `stats.js`는 **순수 함수만**. DOM·fetch·전역 금지 → 단위 테스트로 보증한다.
- 계량 기준(강수 구간, 임계값, 모델 목록)은 전부 `config.js`. 로직에 흩뿌리지 말 것.
- 레이어 분리: 계산(stats) · 통신(api) · 표현(format) · 렌더(ui) · 조율(main).
- 새 기능은 TDD: `test/*.test.mjs` 먼저. `node --test`가 초록인지 확인 후 렌더.

## 검증 방법 (이 환경 특수사항)

- 사무실 PC에서는 **dev-browser 실행이 차단됨**(V3 백신 추정, .exe 스폰 Access denied).
  그래서 브라우저 대신 `scripts/dom-verify.mjs`(jsdom + 실데이터)로 렌더 경로를 검증했다.
- **맥에서는 dev-browser가 될 가능성이 높다.** 되면 실제 브라우저 스크린샷으로 UX 검증할 것
  (유저 성향이 "UX/UI 치밀한 검증"이라 이 단계 생략 금지 — claude-toolkit CLAUDE.md 규칙).

## 현재 상태 (MVP 완료)

- [x] 강수 확률 분포(시간별 24h) + 바람 스프레드
- [x] 전국 시군구 검색 + 즐겨찾기 2곳 + 현재위치
- [x] 판정 문장·상세 패널·표 보기(접근성)
- [x] 단위 테스트 15개, 렌더 검증 16개 통과

## 다음 후보 (우선순위)

1. **기상청 공식 예보 기준선 얹기** — 공공데이터포털/기상청 API허브 키 발급(약 1일).
   앙상블 분포 위에 "기상청은 여기"라는 점 하나를 겹쳐, 합의 대비 위치를 보여준다.
   정적 앱이므로 CORS·User-Agent 이슈 시 작은 프록시 필요할 수 있음(→ GH Pages면 서버리스 함수).
2. **강수 유형(비/눈) 분리** — 겨울철. Open-Meteo `snowfall` 멤버 활용.
3. **관측 대조** — 지난 예보 분포 vs 실제 관측으로 "이 앱이 맞았나" 신뢰도 표기.
4. **배포** — 순수 정적이라 GitHub Pages `git push` 한 번. (지금은 로컬만.)

## 주의

- yr.no(MET Norway)는 브라우저에서 User-Agent 헤더 못 넣어 정적앱 직접호출 불가 → 앙상블만으로 충분.
- `preview.html`은 생성물(정적 스냅샷). 소스 아님. `node scripts/snapshot.mjs`로 재생성.
