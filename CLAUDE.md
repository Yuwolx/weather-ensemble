# CLAUDE.md — weather-ensemble 이어작업 지침

주력 기기는 맥북프로 M5. 이 레포를 clone하면 이 파일로 이어서 파악한다.

## 이 앱이 존재하는 이유 (설계 의도)

- 기상청 단일 예보 불신 → **여러 독립 모델의 앙상블 멤버를 그대로 세어 확률 분포로** 보여준다.
- **[제품 소울] 확률을 대신 골라주지 않는다.** 경우의 수를 다 펼치고, 어디에 걸지는 유저가 고른다.
  틀려도 "예보가 배신했다"가 아니라 **"작은 확률이 이겼구나"**를 느끼게 하는 게 목표(수동적 피해자 → 능동적 베팅 주체).
- **단일 헤드라인 숫자 금지.** "강수확률 80%" 한 줄은 기상청과 똑같다 = 실패. 여러 시나리오와 각 확률
  ("42% 안 옴 · 50% 약한 비 · 8% 비")을 **동시에** 보여주는 게 화면의 주인공이어야 한다.
- **모델 불일치 = 핵심 정보.** 막대가 여러 색으로 쪼개질수록 예보가 불확실하다는 뜻.
- (미래) 결과가 나오면 **어느 시나리오가 이겼는지** 돌아보게 → "작은 확률이 이겼다"의 감정 완성 + 캘리브레이션.
- 디자인 **철학만** 유진분석/위드씨에이에서 계승 — 사용자 명시: "결과물 참고가 아니라 디자인 철학 참고,
  디자인은 우리 앱에 맞게". 철학 = 라이트 단일 화이트, ink #17181c, 라벤더 #5e6ad2는 브랜드 마크 전용,
  **흰 박스 카드·테두리·그림자·좌측 액센트 바·장식용 그라데이션 금지**(AI 템플릿 냄새), 구분은 헤어라인(#e5e7ea)+여백,
  절제, dataviz 색 검증, 정직한 데이터·겸손한 톤.
- **공기(Air) 개정(2026-07-10, 사용자 요청 "딱딱하다, 부드럽고 감성적으로")**: 활성 날짜의 예보가
  하늘 워시를 깐다(clear=따뜻한 아이보리, unsettled=중성 미색, rainy=서늘한 블루그레이) + 라운드
  형태 + 숨 쉬는 모션. **장식용 그라데이션은 여전히 금지, 데이터가 만드는 공기 워시만 유일한 예외.**
  스펙: `docs/superpowers/specs/2026-07-10-air-design.md`.
- **숨 쉬는 하늘 + 날씨의 결(2026-07-10, 사용자 "색감만으론 아쉽다, 더 감성적으로")**:
  body::before = 하늘이 22초 주기로 아주 천천히 부풀었다 가라앉는 앰비언트(breathe),
  body::after = 무드별 질감 — rainy는 초저채도 빗결이 느리게 내림(마스크로 상단만),
  clear는 우상단 햇살 번짐(bloom). unsettled는 숨만. 전부 reduced-motion 존중.
  ※ 질감 알파는 0.13/0.26 수준 — 더 올리면 키치로 넘어가니 주의.
- **고급화(정갈한 관측소) 패스(2026-07-10, 사용자 "둥글둥글 AI 말고 깔끔하고 고급지게")**:
  고급스러움 = 장식이 아니라 **통제**(절제·여백·타이포·정밀 디테일·조용한 모션)로 정의하고 적용.
  ①한글 폰트 **Pretendard Variable**(CDN) — 맑은고딕 탈출이 최대 가성비 ②라운드 다이어트:
  알약 세그먼트→에디토리얼 텍스트 탭(잉크 밑줄), 검색·버튼 9px, 시나리오 박스 제거
  ③시나리오 = 헤어라인 행 + **가는(5px) 계기 트랙** + 타이포 위계(우세 % 26px, 나머지는 옅게)
  ④모션 더 느리고 조용하게(0.8–0.9s). 참고: 고급 레퍼런스는 Mobbin/Refero/Godly에서 유저가 선정 예정.
- **"하늘 한 장" 레이아웃(같은 날, 사용자 "은은한 워시 안 보임 + 레이아웃 구림" 피드백으로 증폭)**:
  좌측 컨트롤 레일 폐지 → 중앙 단일 칼럼(max 780px), 하늘 그라데이션이 **화면 전체**를 채우고
  아래로 갈수록 흰색에 안착. 시나리오 = 하늘 위에 뜬 반투명 흰 밴드(우세는 가장 밝고 % 22px),
  오늘/내일 = 알약 세그먼트, 검색 = 알약 입력, 상단바(브랜드+지역 컨트롤) 한 줄.
  `--p-dry`를 #dce0e5→#aeb7c2로 교정(트랙 회색과 사실상 동일해 안 보이던 dataviz 실수).
- 그 위에 **날씨앱 고유 정체성**: 앱 셸(좌 컨트롤 레일 + 우 계기판 캔버스), 데이터가 색을 끌고 옴
  (강수=파랑 시퀀셜 램프, 기온=따뜻한 앰버 밴드), 숫자 모노스페이스, 로드 모션·LIVE·오늘/내일 탭.
  ※ 초안 반려 이력: ①다크(Linear) ②리포트 겉모습 복제→"보고서 같다". 참고 토큰: `유진분석/design/linear_DESIGN.md`.

## 방향 결정 (2026-07-10, 사용자 확정)

- **수익화하지 않는다. 무료 유용 서비스로 간다.** 니치 B2C/B2B 과금 경로를 검토한 뒤 사용자가 명시적으로 결정.
- 이 결정으로 단순해지는 것: Open-Meteo **무료(비상업) 티어 그대로 사용 가능**, 기상예보업 등록(기상산업진흥법) 이슈 없음, 결제·백엔드 인프라 불필요 → 정적 PWA 유지.
- 프로젝트의 성과 척도는 매출이 아니라 **실제로 유용한가 + 포트폴리오 가치**(불확실성 정량화·캘리브레이션·TDD·설계 문서화).

## 아키텍처 원칙 (지킬 것)

- `stats.js`는 **순수 함수만**. DOM·fetch·전역 금지 → 단위 테스트로 보증한다.
- 계량 기준(강수 구간, 임계값, 모델 목록)은 전부 `config.js`. 로직에 흩뿌리지 말 것.
- 레이어 분리: 계산(stats) · 통신(api) · 표현(format) · 렌더(ui) · 조율(main).
- 새 기능은 TDD: `test/*.test.mjs` 먼저. `node --test`가 초록인지 확인 후 렌더.

## 검증 방법 (이 환경 특수사항)

- 사무실 PC의 dev-browser: 과거엔 전면 차단(V3 백신, .exe 스폰 Access denied)이었으나
  **2026-07-10 현재 동작함.** 단 간헐적으로 spawn EPERM이 나며, 차단이 **수 분간 이어질 때도 있음**
  — 백그라운드에서 `until dev-browser run ...; do sleep 20; done` 재시도 루프가 실전에서 유효했음(10회째 성공 사례).
  headless로 데스크톱(1280×800)·폰(390×844) 스크린샷 + 탭/시나리오 클릭까지 실검증 완료.
- 빠른 회귀엔 여전히 `scripts/dom-verify.mjs`(jsdom + 실데이터)가 1차 관문.
- 실브라우저 스크린샷 UX 검증은 렌더/디자인 변경 시 생략 금지
  (유저 성향이 "UX/UI 치밀한 검증" — claude-toolkit CLAUDE.md 규칙).

## 현재 상태 ("대폭 단순화" 커밋 2d3718d 이후 기준)

- [x] **시나리오 보드(히어로)** = 선택 시각의 경쟁 확률 전부(단일 % 없음) + 기온/강수량/바람 한 줄.
  **[원칙 개정 2026-07-10]** 탭 = **예감 선언**: 시각별로 localStorage에 저장되고(pick:),
  다음날 돌아보기에서 채점되어 통산 적중률이 쌓인다(사용자: "본인의 예감 적중률을 비교").
  과거 "저장·결과대조 없음" 조항은 이 사용자 요청으로 폐기. 단 **포인트·결산판 같은 베팅 UI는
  여전히 금지** — 채점은 돌아보기의 조용한 정보 줄로만.
- [x] 시간별 스트립(강수 확률 스택 막대, 오늘/내일 탭, 호버 툴팁, 키보드 이동) + 표 보기(접근성)
- [x] 전국 시군구 검색 + 즐겨찾기 2곳 + 현재위치, PWA(홈화면 설치·오프라인 셸)
- [x] **공기(Air) 시스템(2026-07-10)**: `dayMood()`(stats, 순수·TDD) → `<html data-mood>` →
  CSS 하늘 워시(@property로 크로스페이드). 라운드 막대/트랙, 0% 시나리오 빈 트랙(정직),
  선택 하이라이트 = 데이터 블루 옅은 채움(라벤더는 브랜드 마크로 복귀), 부드러운 모션,
  **스트립 밑 둥근 점 범례**(모바일 호버 부재 해결).
- [x] **돌아보기(회고) v2(2026-07-10)** — 세 층의 조용한 장부: ①**어제 실제**(항상,
  `loadActualsYesterday` = forecast API past_days=1 재분석 근사; `summarizeActuals`)
  ②**그때의 경우의 수**(스냅샷 있을 때: `snapshots.js`가 기기가 본 분포를 **최초 1회만** 저장
  first-seen wins, `pickRetroHour`+`retroVerdict`로 "작은 확률이 이겼다" 문장+미니바)
  ③**당신의 예감**(`settlePicks`로 채점 → `hunch-record`에 통산 누적, "통산 3/5 적중" 표기).
  **[정정] Ensemble API past_days는 문서 요약과 달리 실측 결과 진짜 예보 스프레드를 보관함**
  (어제 22시 멤버 0.0~1.2mm 분포 vs 실제 7.3mm — 동화된 사후값이면 불가능한 스프레드).
  → 콜드스타트: 스냅샷 없으면 이 보관 예보로 "어제 — 예보가 봤던 경우의 수"를 첫 방문부터 표시,
  스냅샷("그때 화면의 경우의 수")이 있으면 그게 우선. 메인 호출에 past_days=1 동승(추가 요청 0).
- [x] **레인 아트(2026-07-10)** — 선택 시각의 우세 시나리오가 비면 화면에 비가 내린다.
  최종 구현은 **`js/rain.js` 캔버스**(방울마다 길이·속도·투명도 랜덤, 아래로 갈수록 소멸,
  강도별 밀도/속도, visibilitychange 일시정지, reduced-motion·jsdom 무동작 가드).
  시행착오 3번 기록: ①gradient 축 실수 → 가로 밴드 ②연속 사선 → 노트 빗금
  ③SVG 타일 반복 → 패턴 티 남("비오는거 구리다") → **타일이 아니라 캔버스 입자가 정답.**
  `data-rain` 속성은 상태 훅으로 유지(dom-verify·CSS). 스트립을 눌러 다니면 날씨가 연주됨.
- [x] **돌아보기 그래프 대조(사용자 제안, v3)** — 세 줄이 시간 정렬로 쌓임:
  ①어제의 예보 분포 미니 스트립(스냅샷 우선, 없으면 보관 예보 — 캡션으로 출처 구분)
  ②**예감 행**(유저가 골랐던 시각에 색점) ③**실제 리본**(시간별 실제 버킷 색).
  판정 한 줄은 결과의 사전 확률이 주인공: "그때 이 경우의 확률은 **6%**. 작은 확률이 이겼습니다."
  (주의: 사용자 구어 '~짜리'를 UI에 그대로 쓰지 말 것 — 지적받음. 리본 툴팁도 '그때 확률 N%'.)
- [x] **어제의 예보 성적(2026-07-10)** — `forecastScore`(순수·TDD): 대조 가능한 시간 중
  우세 시나리오 적중률 + **실제 일어난 날씨에 줬던 평균 확률**(후자가 더 정직한 지표).
  "24시간 중 11시간 우세 적중(46%), 평균 35%" 식으로 돌아보기에 한 줄. 낮은 날도 그대로 보여줌 —
  그게 이 앱의 존재 이유.
- 숫자 표 보기 제거(사용자: "굳이 필요할까") — renderTable·토글·CSS 전부 삭제.
- [x] 단위 테스트 32개, 렌더 검증 29개 통과 + 실브라우저(데스크톱/폰, 오늘 unsettled·내일 clear) 확인
- ⚠ **sw.js 교훈**: SW network-first여도 브라우저 HTTP 캐시가 모듈 버전을 섞을 수 있음
  (새 main.js + 옛 config.js로 부팅 사망 실측). fetch `cache:'no-cache'` + 셸 변경 시 CACHE 버전 범프 필수.
- 단순화 커밋에서 제거된 것: 기온 p10–p90 밴드 차트, 레일 판정문/미니라인, 하루 요약, 바람 그래프.
- **남은 알려진 이슈**: 실브라우저 검증은 이 PC(headless)로 완료, 실기기(폰 PWA) 확인은 미실시.

## 배포 (준비 완료, 스위치만 남음)

- **정적 서빙 준비 끝**: 모든 경로 상대(`./`), manifest scope `./`, `.nojekyll` 추가,
  SW ASSETS 최신(v4). GitHub Pages 서브패스(`/weather-ensemble/`)에서 그대로 동작.
- **절차**(사용자만 가능): ①레포 Settings → General → 공개(public) 전환
  ②Settings → Pages → Source: Deploy from a branch → main / (root) → Save
  ③1~2분 후 `https://yuwolx.github.io/weather-ensemble/` 접속, 폰에서 "홈 화면에 추가".
- 공개 전환이 싫으면 대안: Cloudflare Pages/Netlify 무료(private 레포 연결 가능, 계정 연결 필요).

## 모바일 접근성 (2026-07-10 정비)

- 폰(≤560px)에서 시간별 스트립이 **가로 스크롤**(min-width 640px, 화면 끝까지 블리드,
  스크롤바 숨김) — 막대 탭 타깃이 ~13px에서 24px+로. 키보드 화살표 이동은 그대로.
- 노치/홈바 safe-area 패딩(`env(safe-area-inset-*)`, viewport-fit=cover) 반영.
- 캔버스 비: reduced-motion·백그라운드 탭 정지, DPR 상한 2(배터리).

## 다음 후보 (무료 서비스 방향 기준, 업데이트)

1. **배포 스위치**(위 절차, 사용자 결정 대기).
2. **회고 확장** — v1(어제 한 시각)을 넘어: 여러 날 누적 캘리브레이션("30%라던 날 실제로
   10번 중 3번 왔나"), 어제 전체 시간대 미니 스트립. 스냅샷이 쌓여야 의미 → 기기당 14일 보관 중.
3. 기상청 공식 예보 기준선 얹기(공공데이터 키 ~1일), 비/눈 분리(겨울).
- 위치: `Desktop\취업진로\weather-ensemble` · 원격 **github.com/Yuwolx/weather-ensemble (private)**

## 작업 습관 (반드시 지킬 것)

- **작업 단위마다 commit + `git push`.** 사용자 지시: "중간중간 푸시하고 문서화 다 해놔."
  큰 변경을 한 덩어리로 몰지 말고, 의미 있는 단위로 자주 올린다.
- 기능/디자인/의사결정이 바뀌면 **이 CLAUDE.md의 현재 상태·이력을 같은 커밋에서 갱신**한다.
- 커밋 메시지는 한국어 보고체 + 근거. 사용자 승인 없이 강제푸시·히스토리 변경 금지.
- 사용자 피드백 반영 이력: 다크→라이트, 보고서느낌→앱셸, 가시성↑, 기온/강수량 추가,
  차트 축소, 시간축 눈금(2h), 바람 그래프 복원, "멤버수 문구" 제거, 대폭 단순화(경우의 수만),
  **수익화 안 함·무료 서비스 확정(2026-07-10)**, **"딱딱하다" → 공기(Air) 개정(2026-07-10)**,
  **"워시 안 보임·안옴 안 보임·레이아웃 구림" → 하늘 한 장 개편(2026-07-10)**.

## 검증 커맨드 (이어받을 때 먼저 실행)

```bash
node --test                 # 순수 로직 29개 (죽은 verdict/timeOfDay 제거로 감소)
node scripts/dom-verify.mjs # 실데이터 렌더 29개 (jsdom 1차 관문)
python -m http.server 8848  # 로컬 구동 → localhost:8848
```

## 주의

- yr.no(MET Norway)는 브라우저에서 User-Agent 헤더 못 넣어 정적앱 직접호출 불가 → 앙상블만으로 충분.
- `preview.html`은 생성물(정적 스냅샷). 소스 아님. `node scripts/snapshot.mjs`로 재생성.
